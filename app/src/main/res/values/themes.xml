<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Tail" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
        Transparent theme for ShareTextActivity.
        The activity itself is invisible — only the Compose Dialog is shown,
        making it look like a floating share sheet rather than a full screen.
    -->
    <style name="Theme.Tail.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

</resources>